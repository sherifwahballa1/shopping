const addToCart = require("./add-to-list");
const cartList = require("./get-list");
const removeFromList = require("./remove-product");

module.exports = {
  addToCart,
  cartList,
  removeFromList,
};
